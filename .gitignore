# macOS
.DS_Store

# Node / Playwright
node_modules/
test-results/
playwright-report/
.playwright/

# Private overrides (token, local secrets — never commit)
config.private.js
.claude/settings.local.json
